<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ServoIO.View.MasterDetailPageIODetail"
             Title="Detail">
    <!--<StackLayout Orientation="Horizontal">
        <StackLayout Orientation="Vertical" HorizontalOptions="StartAndExpand">
            <Picker x:Name="startYear" ItemsSource="{Binding StartDate}" Title="Start Year" HorizontalOptions="Fill" WidthRequest="100"  >
            </Picker>
        </StackLayout>
        <StackLayout Orientation="Vertical"  HorizontalOptions="StartAndExpand">
            <Picker x:Name="endYear" Title="End Year"  ItemsSource="{Binding EndDate}" HorizontalOptions="CenterAndExpand" WidthRequest="100"  >
            </Picker>
        </StackLayout>
        <StackLayout>
            <Button Text="Submit" Command="BtnSubmit" x:Name="btnSubmit" Clicked="btnSubmit_Clicked"></Button>
        </StackLayout>
    </StackLayout>-->

</TabbedPage>

